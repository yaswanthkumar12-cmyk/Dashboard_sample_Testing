title = "Gitleaks Full Scan"
description = "Scan all files for secrets"

# Example rule to detect anything containing 'SECRET' for testing
[[rules]]
id = "test-secret"        # Unique ID is mandatory
description = "Detect test secrets"
regex = "(?i)SECRET"     # Case-insensitive match
tags = ["test"]
